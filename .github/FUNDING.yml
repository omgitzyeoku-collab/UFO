# One channel, matching the /donate page. Six "coming soon" links were removed for
# the same reason they were removed from the site: on a project whose only asset is
# doing what it says, half-wired funding reads as unfinished.
#
# This shows a "Sponsor" button on the repo the moment GitHub Sponsors is enabled on
# the omgitzyeoku-collab account. Until then GitHub simply hides the button — it does
# not error — so this is safe to commit now and needs no further edit.
#
# When the Open Collective fiscal host is approved, add its slug here as a second
# line (open_collective: <slug>) — that unlocks organisational sponsors.
github: omgitzyeoku-collab
